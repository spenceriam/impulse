# tool_docs

Reads built-in tool documentation from the Impulse tool library.

## Parameters

- name (optional): Tool name (e.g. file_read, bash)
- list (optional): List available tool docs

## Usage

- Call with list=true to see available tool docs
- Call with name to retrieve a specific tool's documentation
