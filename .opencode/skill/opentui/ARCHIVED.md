# ARCHIVED — OpenTUI Skill

This skill documents the **removed OpenTUI + SolidJS UI stack** (pre-v1.0 CLI rework).

**Do not use for new work.** The active terminal UI lives in `src/cli/**` and uses `@mariozechner/pi-tui`.

For current patterns, see:

- `src/cli/renderer.ts` — main TUI shell
- `src/cli/components/` — overlays, context bar, tool blocks
- `src/cli/layout.ts` — narrow-pane overlay width helpers
- `.opencode/skills/frontend-design/` — brutalist terminal design patterns (still relevant)

Historical references in this directory are kept for archaeology only.
